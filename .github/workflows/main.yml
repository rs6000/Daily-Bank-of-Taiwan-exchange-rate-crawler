name: Daily Exchange Rate Crawler (BOT - TPE Time)

on:
  # 每日排程觸發：
  # 這裡設置為每天 UTC 時間 01:00 執行。
  # 換算成台北時間 (GMT+8) 約為 上午 9 點
  schedule:
    - cron: '0 1 * * *' 
  
  # 允許您隨時手動點擊按鈕來運行工作流 (方便測試)
  workflow_dispatch:

jobs:
  update-rates:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 檢查程式碼
      - name: Checkout repository code
        uses: actions/checkout@v4
        # 關鍵：使用 GITHUB_TOKEN 讓 Action 有權限提交回儲存庫
        with:
          token: ${{ secrets.GITHUB_TOKEN }} 

      # 2. 設定 Python 環境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' 

      # 3. 安裝爬蟲所需的依賴
      - name: Install dependencies (pandas, html5lib, pytz)
        # 確保安裝了所有必要的函式庫來執行 crawler_bot.py
        run: pip install pandas html5lib pytz
        
      # 4. 執行爬蟲程式
      - name: Run Crawler and Generate JSON
        # 執行您的 Python 檔案
        run: python Exhange-Rates-Gemini.py
        
      # 5. 自動提交和推送變更
      - name: Commit and Push new data
        # 使用專門用於自動提交的 Action
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          # 只提交生成的 JSON 檔案
          file_pattern: 'data/rates.json'
          # 提交訊息
          commit_message: '🤖 Daily BOT Rate Update (TPE Time): ${{ github.event.repository.name }}'
          # 設置提交者的名稱和郵件地址
          commit_user_name: 'GitHub Action Bot' 
          commit_user_email: 'action@github.com'
